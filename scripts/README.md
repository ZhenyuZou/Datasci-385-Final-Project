# Scripts

This folder is reserved for SQL and Python scripts used in the analysis.

The current analysis is primarily contained in `Datasci385_Final_Project.ipynb` and `index.qmd`. Future versions can separate SQL cleaning and Python visualization code into this folder for improved reproducibility.