# Data

This folder is intended to store datasets used in the project.

The current database file (`wdi_population.db`) is kept in the root directory to ensure compatibility with existing scripts and the Quarto report.